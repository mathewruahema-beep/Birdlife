# Credentials — never commit. The WooCommerce key leak started as a value pasted
# somewhere it was not supposed to live.
.env
*.env
!.env.example

# Extract output — regenerate with powerbi/extract/extract.py, don't version it.
# These CSVs contain finance and helpdesk records.
powerbi/extract/out/
*.csv
# ...but the budget template is structure, not data.
!powerbi/budget/budget_template.csv
# The real budget is finance's figures — keep it out of git.
powerbi/budget/budget.csv

# Power BI working files
*.pbix
*.pbip

__pycache__/
*.pyc
