# 2026-09-12

## Decisions
- **Pull the rest of the account brain from Mathew's personal Google Drive, not OneDrive.** The folder `C:\Users\mathe\OneDrive\GoogleDrive\Claude` syncs to his personal Microsoft OneDrive and his personal Google Drive; the BirdLife tenant never had it. Decided by Mathew ("see if you can reach google drive now"). Source: this session.
- **Import the August plugin's birdlife-core as the doctrine under the charter, not instead of it.** `CLAUDE.md` wins on conflict; birdlife-core's four layers are mapped onto the repo in a note at the top of that skill. Decided by the session under the import instruction; Mathew to confirm or reverse. Source: this session.
- **ADRs are not renumbered.** Two carry 0019 and 0017 is missing; both facts are recorded in birdlife-core, the decision register (registers section 7) and here. The next ADR is 0021. Rule 3 of birdlife-core (never edit an ADR) applied. Source: this session.
- **Not imported, on purpose:** the August `birdlife-google-workspace` skill (superseded by `google-workspace-expert`), `birdlife-spotify` (personal, no business function), the 11 Aug `birdlife-ict-brain-repo.tar.gz` snapshot, and the Word, PDF and PowerShell documents in the Drive library (indexed by name in `INDEX.md`, not committed). Source: this session.

## Done
- birdlife-core copied from `birdlife-ict.plugin` (v1.1.0, Aug 2026): SKILL.md, ADRs 0001 to 0016, fact-classification, verification-queries (8 Aug baselines), six knowledge digests. ADRs 0018 (break-glass), 0019 (Birdata feed), 0019 (lens model, via patch 0003) and 0020 (Salesforce Outlook pilot) added.
- Patches applied with `git am --3way`, all clean, original authors and dates kept: 0002 (birdlife-manager plus `docs/manager/` review and learning log of 7 Sep), 0003 (`os/lenses.md` and the lens ADR), and the 3 Sep registers orphan-check patch that had also never reached the repo. Patch 0001 (prompting) was byte-identical to the account copy already in the repo.
- Seven connector skills added from the plugin: atlassian, canva, gmail, google-calendar, granola, microsoft-learn, miro.
- `os/claude-os-overview.html` replaced with the 8 Sep version carrying the Lens tab, so the repo source matches the live artifact again.
- Plugin README, GOVERNANCE and CONNECTORS kept under `docs/brain-v1/` as history.
- Charter table, `INDEX.md`, skill register (33 rows), artefact register, credential watchlist (the 2024 `admin365.ross` Zapier Teams connection, finding F2), memory register and a new decision register (section 7) updated. Desktop task card withdrawn.
- Secret scan of everything imported: key-shaped strings and webhook URLs, none found. Word-level hits were prose.

## Learned
- **The Google Drive connector reaches what the M365 connector cannot.** `GoogleDrive/Claude` is Mathew's human-copy layer for the whole brain: documents, patches, plugin builds, archived artifacts. The M365 connector's OneDrive is the tenant one and does not contain it. Doctrine: `INDEX.md`, "Not in the repo".
- **Every "repo-inbox" patch was a clean `git am`.** The pattern worked; what failed was that nobody ran it for five days. The fix is not a better inbox, it is a repo session that checks the inbox, which the weekly audit now does through the account-skill step and this journal.
- **Patch 0001 was already in the repo because the account copy was pulled yesterday.** Pulling the account first and the inbox second is the right order: the account has the newest skill bodies, the inbox has the supporting files and ADRs the account cannot hold.
- **Mathew's own words, 11 Sep late:** he wants to run the brain from his Google Drive rather than BirdLife so he retains it. The session's read: the brain is already entirely under his personal accounts (GitHub `mathewruahema-beep`, Google Drive `mathew.rua.hema@gmail.com`, claude.ai account), so this is not a migration but a naming of what is true, with one consequence to weigh: BirdLife's confidential operating knowledge then has no organisational home, and Andrew, Keith and Nina inherit nothing if he leaves. Recorded as an open decision with a recommendation, not implemented.

## Open
- **Where the brain lives (Mathew).** Recommendation: the git repository stays the executable brain (Claude Code loads `CLAUDE.md`, skills and memory from it; Google Drive cannot be loaded at session start and has no diff or history), `GoogleDrive/Claude` becomes the declared human-copy layer, and OneDrive is dropped from the skills' file paths. If Mathew wants BirdLife to have no claim, that is a conversation with BirdLife about IT-GOV-002 (the AI policy) and his employment terms, not a file move. Needed before the two-brains decision closes.
- Still not found: `salesforce-delivery-governance` skill, ADR 0017, IT-SEC-002/003/004/007, IT-WEB-001. Mathew to say whether they exist.
- `birdlife-manager` and `birdlife-core` are not on the claude.ai account; the 7 Sep note said manager was saved and the live list disagrees.
- Skill file paths in birdlife-manager, birdlife-prompting, the plugin's atlassian skill and os/lenses.md still say `OneDrive Birdlife\Claude\...`; the real folder is `GoogleDrive\Claude`. Fix in the skills once Mathew confirms the Drive decision.
- IT-SF-018 (Payments2Us operator reference, 68 KB Markdown) and the IT-SF-016 role pack exist in Drive as Markdown and could be committed as references; not done today.
- The `admin365.ross` Zapier Teams connection (2024, shared credential) is on the watchlist and needs removal.

## Decisions (later)
- **ADR 0021, Accepted.** The git repository is the executable brain; Mathew's personal Google Drive `Claude` folder is the human copy and weekly bundle backup; OneDrive is out; the repo-inbox is retired; ownership is Mathew's personally. Decided by Mathew ("I want to keep 1 and ownership is mine in my personal drive for 2"). Source: this session.

## Done (later)
- ADR 0021 written; every skill and document path that said `OneDrive Birdlife\Claude` now says `GoogleDrive\Claude`; the birdlife-os repo-inbox instruction replaced with "commit from the local clone"; charter, OS estate model (human copy row, rule 9) and registers updated.

## Learned (later)
- **The repository has been public since 2 Aug 2026.** GitHub reports visibility public, Pages enabled, 33 branches, 0 forks, 0 stars. It carries the confidential security register, tenant IDs and named privileged accounts. Nobody checked visibility when the brain was moved into git. Recorded on the credential watchlist and in ADR 0021; the fix is Mathew's in the GitHub UI. Pattern: `memory/patterns/claude-estate.md`.
- There is no `main` or `master` branch; the default branch is `claude/code-connectors-example-5v1x7n`, and the weekly audit routine is told to push to "the default branch". Many of the 33 branches are unrelated personal projects (voice coach, wallpaper, chat app). Pruning and a real default branch are part of the same cleanup.

## Open (later)
- **Mathew, today:** make the repository private, disable Pages, prune the unrelated branches, set a default branch (recommend creating `main` from this branch once the brain work merges). Then run the leaked-document sweep over the history.
- Weekly `git bundle` to `GoogleDrive/Claude/repo-backup`: manual for now; adding it to the Monday audit is an estate change (Google Drive must be attached to that routine in the UI).

## Done (evening)
- **The BirdLife Brain map built and published** at https://claude.ai/code/artifact/7c0faca1-812a-4ce2-8b0e-b9d501ef95d3, source committed as `os/birdlife-brain.html`, artefact register and INDEX rows added. Asked for by Mathew ("an electronic representation of the brain similar to the one in i, Robot, everything in different colours"). Canvas page, five rings out from CLAUDE.md, 13 colour-coded layers (charter, index, doctrine, OS rules, registers, lenses, console, memory rules, skills, journals and patterns, routines, ADRs, live systems, artefacts, human copy), pulses along the "reads" edges, legend click to isolate a layer, click a node for its inspector card, search, pan and zoom. Generated from `braindata.json` built off the repo at commit 8036c72.

## Learned (evening)
- **The map is a snapshot, not a live view.** It reads nothing at runtime; the node list is baked in from the registers at build time, so every estate change makes it a little wrong until rebuilt. That is the right trade for a page with no connectors and no write surface, but the artefact register row now says so and names the rebuild trigger.

## Open (evening)
- If Mathew wants the map to stay true without rebuilds, the next step is to have it read `os/registers.md` and `INDEX.md` from the repository at load time, which needs the repo private first (ADR 0021 remediation) and a decision on how the page authenticates to GitHub. Not started.

## Done (evening, later)
- **The brain map now reads the repository instead of a snapshot.** Mathew: "Make it live so it reads the repo and not the snapshot." Same artifact URL, republished with the `mcp` capability declaring one connector and one read-only tool: GitHub `get_file_contents`. At every open it reads CLAUDE.md, INDEX.md, `os/registers.md` and the skills, journal, patterns, decisions and os folders on the branch in the page's Branch field, builds the graph from what it finds, refreshes every five minutes, and reads a file's heading when its node is clicked. Parsers checked against the live files: 11 recurring plus 2 one-shot routines, 7 artefacts, 19 connectors, 33 skill descriptions. Verified in this session: the connector's real responses for a file read and a directory listing (the shapes the page handles). Not yet verified: a viewer opening the page and granting GitHub, which is Mathew's first open.

## Learned (evening, later)
- **A page can read the brain without a secret.** The artifact runtime calls the viewer's own GitHub connector, so the page holds no token and a viewer without GitHub sees the structure and a plain reason. That removes the "repo must be private first" dependency noted earlier today for a live map; the repository still needs to be made private for the reasons in ADR 0021.
- **The registers are now an interface, not just a record.** The map parses section 1 (routine tables), section 3 (bold connector names and the "Also connected" sentence) and section 4 (artefact table) of `os/registers.md`, and the skill table in CLAUDE.md. Changing those formats now breaks a consumer. Recorded in the artefact register row.

## Open (evening, later)
- Mathew: open the map, allow GitHub when asked, confirm the node counts match the registers. If the connector's display name is not accepted as "github", the banner will say so and the manifest needs the exact display name.
- When `main` exists, type it into the Branch field once (it is remembered in the browser) and update the default in `os/birdlife-brain.html` and the register row.
