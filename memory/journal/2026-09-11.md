# 2026-09-11

## Decisions
- **The repo is the brain; do not build a separate knowledge platform.** The estate already sprawled twice when things were added without governance; a second memory would drift from the skills. Decided by Mathew ("Build it, steps one to four"). Source: this session, branch `claude/birdlife-brain-build-02zobt`.
- **Episodic memory lives in git (`memory/`), not the console's artefact database.** Versioned, greppable, readable by the ICT team without connector grants, no credentials by rule. Decided by Mathew. Source: this session.
- **The session close rule is mandatory** (journal entry plus skill or facts edit in the same commit, for any session that changed something). Added to `CLAUDE.md`. Decided by Mathew. Source: this session.
- **Facts files sit beside the prose, not instead of it.** Rewriting sixteen skills to strip IDs out of prose was judged too risky in one pass; the rule is facts file first, prose second. Decided by the session under the build instruction; Mathew to confirm or reverse. Source: this session.

## Done
- `memory/` created: README (rules and formats), journal backfilled for 7 Aug, 2 Sep and 3 Sep from the registers and git log, patterns seeded for Salesforce, the website and the Claude estate.
- `references/facts.md` added to eight system skills (salesforce, asana, microsoft365, netsuite, wordpress, stripe, zapier, cloudflare), values copied from the verified reads already in each skill; a pointer line added under each skill's heading. `birdlife-ict-assistant` keeps `references/reference.md` as its facts file.
- `INDEX.md` created at the repo root; `CLAUDE.md` gained the brain section and the session close rule; `os/README.md` gained the memory and index rows and rule 9; the `birdlife-os` weekly audit gained a memory check; the `birdlife-improvement` learn step gained the journal line; `os/registers.md` gained a memory register and the skill-register note; docs updated.

## Learned
- What the estate had was semantic and procedural memory with no episodic memory and no enforced write-back. The "44.55 USD persistent session" for the Friday status was episodic memory bought with context tokens. Doctrine: `memory/README.md`.
- Mathew's own standard, stated 11 Sep: he wants to be a learning brain too, "always learning, adapting and solving complex tasks". The journal's Learned section therefore records his calls and what he would do differently, not only system gotchas. Doctrine: `memory/README.md`, entry format.

## Decisions (afternoon)
- **Pull the account into the repo before deleting anything from the account.** The live account list showed 23 BirdLife skills against the register's 15, with eight account-only and four newer than the repo. Decided by Mathew ("do 1 and 2"). Source: this session.
- **The account copies were newer, so "repo wins on drift" was wrong for 4 to 11 Sep.** Cowork sessions without a GitHub connection taught the account brain (Zeus group assignment rule 4 Sep, lens model 8 Sep, SC-300 identity baseline 10 Sep, WordPress live read 11 Sep, six new skills 10 to 11 Sep) and queued repo patches in a OneDrive folder the repo never received. The register now says repo wins only once account content is committed. Source: skill diffs, this session.

## Done (afternoon)
- Nine account skills copied into `.claude/skills/` verbatim from the synced copies on disk: birdlife-conga, birdlife-movedata, birdlife-payments2us, birdlife-powerbi, birdlife-prompting, birdlife-sdocs, birdlife-wpengine, google-workspace-expert, morning.
- Newer account versions taken for birdlife-security (SC-300 baseline, P1 versus P2, identity playbooks), birdlife-wordpress (full rewrite from an 11 Sep live read; facts file updated with the new values and the superseded ones bracketed) and birdlife-ict-assistant plus its reference (Zeus public group `00GRF000001s1RZ2AY` as the only valid assignee set).
- birdlife-os merged: account lens model and IT Admin matrix plus the repo's memory audit step, and a new audit step comparing the live account skill list against the repo.
- Seven shared skills confirmed identical to the repo apart from today's pointer line (asana, cloudflare, microsoft365, netsuite, salesforce, stripe, zapier); people-lifecycle, reporting and email-voice identical.
- Salesforce facts gained the production and staging org Ids and the Zeus group rule. `CLAUDE.md` table, `INDEX.md`, the skill register (now 24 rows) and the docs updated.

## Learned
- **The synced account skills are on disk in a repo session** at `/root/.claude/skills/synced/<id>/<skill>/`, with a `manifest.json` carrying each skill's `updatedAt`. Diffing the account against the repo is a shell command, not a UI exercise. Doctrine: `birdlife-os`, weekly audit step 8.
- Seven account skills say "read `birdlife-core` first". No such skill exists in the repo, on the account, or in SharePoint search. The account brain was built against a rule set nobody versioned. Until it is found, `CLAUDE.md` and `birdlife-os` carry that role. Recorded in the skill register.

## Open
- **Locate and commit**: `birdlife-core`, `birdlife-manager`, `salesforce-delivery-governance` (skills); `os/lenses.md`, ADR 0002, ADR 0019, IT-GOV-004 and the other IT-* documents; the OneDrive `Claude\repo-inbox` patches 0001 to 0003 with REGISTER-ROW notes. The M365 connector's view of Mathew's OneDrive has no `Claude` folder (root, Documents and the empty Cowork folder checked 11 Sep); they are probably on the local Cowork machine only. Mathew to point a repo session at them. Needed before the account deletion.
- `birdlife-improvement` has never been uploaded to the account; it is the only repo skill the app cannot see.
- **Two brains** (repo skills versus account-synced copies): stop syncing to the account and always start on the repo, or automate the sync in the weekly audit. Mathew's decision; nothing built for it here. Needed before the next account re-upload.
- Account copies of all skills are now further behind: the zips must include the new `references/` directories when re-uploaded.
- Ingest the June security reviews, July NetSuite review and WordPress health check into the skills (step five of the plan); one session per document.
- Routines do not yet write to the journal; the weekday job and the pre-draft run in fresh sessions with repo access and could, once their prompts are updated (an estate change, propose first).
