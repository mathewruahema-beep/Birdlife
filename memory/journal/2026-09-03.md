# 2026-09-03

Backfilled 11 Sep 2026 from `os/registers.md`, `routines/unused-routines-review-2026-09-03.md` and the git log.

## Decisions
- **Retire the Top 10 morning email draft**; it overlapped the console's Catch me up. Decided by Mathew (morning). Source: registers.
- **Retire the weekend dashboards job and the Friday weekly status update.** The weekend job's 29 Aug fire stalled 4.5 days and produced nothing; the Friday job was bound to a persistent session that had consumed 506k context tokens (44.55 USD). Consequence accepted: no weekend dashboard refresh and no automated Security dashboard refresh. Decided by Mathew (evening). Source: unused-routines review.
- **Keep the department suite and the Ops/Monitoring dashboards.** The console does not cover their content and the Teams files are the team's shared view. Decided by Mathew. Source: registers, artefact register cleanup review.
- **Retire the old ICT Console artifact** (tombstoned to a redirect) and remove `console/` and `dashboard/` from the repo. Decided by Mathew. Source: git log dd15786.

## Done
- Weekday dashboards job fixed: website probes moved from the Zapier webhook action to WebFetch; Zapier Teams post moved to last so a held approval cannot take the SharePoint push down.
- Overnight pre-draft fixed: prints the full draft pack as text before attempting the Salesforce create.
- DST fix one-shot rewritten against the current 11 recurring routines.
- AI Daily Ten routine registered (created outside the register earlier that day).
- Console rebuilt as the command deck; Money, Security and Fixes tabs added; case assign with duplicate-User safeguard.
- Four skills created: `birdlife-security`, `birdlife-people-lifecycle`, `birdlife-reporting`, `birdlife-improvement`. Expert pass over every repo skill with observed connector behaviour.
- `os/` merged to the default branch so the weekly audit can find the register.

## Learned
- **Firing but idle**: a connector write held for approval in an unattended auto-mode session stalls the whole run and the routine reports success. Three routines were affected. Pre-approval and push notifications can only be set in the Routines UI, not the API. Doctrine: `birdlife-os` and `os/registers.md` findings. Pattern: `memory/patterns/claude-estate.md`.
- Connector display names for the artifact `mcp` capability need the spaces ("Salesforce Production"). Doctrine: `birdlife-os`, updating the console.
- There are five livemode Stripe accounts, not one. Doctrine: `birdlife-stripe`.

## Open
- Mathew's Routines UI list: pre-approve the Zapier action on the weekday job and createSobjectRecord on the pre-draft; turn the pre-draft's push notification on; attach Gmail to AI Daily Ten.
- Verification: heartbeat line dated 4 Sep 07:xx AEST in `_dashboard-run-log.txt` on SharePoint.
- Account copies of every skill are stale until re-uploaded from the repo.
